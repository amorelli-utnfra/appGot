import { IsStarkDirective } from './is-stark.directive';

describe('IsStarkDirective', () => {
  it('should create an instance', () => {
    const directive = new IsStarkDirective();
    expect(directive).toBeTruthy();
  });
});
